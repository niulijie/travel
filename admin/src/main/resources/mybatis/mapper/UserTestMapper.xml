<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.admin.mapper.UserTestMapper">
    <select id="getUserTest" resultType="com.atguigu.admin.bean.UserTest">
        select * from user_test where user_id = #{userId}
    </select>

    <!--<insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        insert into user_test (name,age,grade,sex,create_time,update_time) values (#{name},#{age},#{grade},#{sex},#{createTime},#{updateTime})
    </insert>-->
</mapper>